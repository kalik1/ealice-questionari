import { Route } from '@angular/router';
import {UtentiComponent} from './utenti.component';

export const utentiRoutes: Route[] = [
    {
        path     : '',
        resolve: {

        },
        component: UtentiComponent
    }
];
